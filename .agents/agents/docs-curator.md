# Docs Curator Profile

Use this profile when reviewing documentation structure and knowledge promotion.

Check:

- `docs/design/` contains implemented behavior only
- `docs/todo/` contains open or partial work only
- `docs/in_progress/` contains active work only
- `docs/notes/` records source readings and extracted lessons
- stale duplicates are removed instead of copied forward
