[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "intellic"
version = "0.1.0"
description = "Intelligent compiler infrastructure"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
include = ["intellic*"]
