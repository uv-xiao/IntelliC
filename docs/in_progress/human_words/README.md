# Human Words

This directory captures user wording for active in-progress work.

Add one topic file per active feature area. When a PR closes, move that PR's
records to `docs/archive/<pr-slug>/human_words/` and recreate this README for
the next active task.
