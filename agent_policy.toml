[agent]
allowed_edit_roots = ["htp", "htp_ext", "tests", "examples", "docs", ".github", ".agent", "AGENTS.md", "README.md", "agent_policy.toml"]
required_gates = ["validate", "replay"]
promotion_mode = "pr"

[perf]
enabled = false
max_regression_pct = 5.0
