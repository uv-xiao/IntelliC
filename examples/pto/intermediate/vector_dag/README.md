# PTO PyPTO Vector DAG

This example compresses the PyPTO vector-DAG arithmetic story into one fused
HTP kernel while keeping the source code as ordinary Python expressions.

Run:

```bash
python -m examples.pto.intermediate.vector_dag.demo
```
